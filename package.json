{
  "name": "chatapp-server",
  "version": "1.0.0",
  "scripts": {
    "install-deps": "cd server && npm install && cd ../public && npm install",
    "build": "cd public && npm run build"
  },
  "dependencies": {
    "express": "^4.18.2",
    "mongoose": "^8.0.3",
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "socket.io": "^4.7.2"
  }
}
